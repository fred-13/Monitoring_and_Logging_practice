UserParameter=redis.version,/bin/echo "$(/usr/local/bin/redis-cli -a Password123 --no-auth-warning info server | grep redis_version: | cut -c15-30)"
UserParameter=redis.used.memory,/bin/echo "$(/usr/local/bin/redis-cli -a Password123 --no-auth-warning info Memory | grep used_memory: | cut -c13-100)"
UserParameter=redis.instantaneous_ops_per_sec,/bin/echo "$(/usr/local/bin/redis-cli -a Password123 --no-auth-warning info Stats | grep instantaneous_ops_per_sec: | cut -c27-100)"
UserParameter=redis.clients,/bin/echo "$(/usr/local/bin/redis-cli -a Password123 --no-auth-warning info Clients | grep connected_clients: | cut -c19-100)"
UserParameter=redis.total_commands_processed,/bin/echo "$(/usr/local/bin/redis-cli -a Password123 --no-auth-warning info Stats | grep total_commands_processed: | cut -c26-100)"
UserParameter=redis.replication.role,/bin/echo "$(/usr/local/bin/redis-cli -a Password123 --no-auth-warning info Replication | grep role: | cut -c6-20)"
UserParameter=redis.used_cpu_sys,/bin/echo "$(/usr/local/bin/redis-cli -a Password123 --no-auth-warning info CPU | grep used_cpu_sys: | cut -c14-100)"
UserParameter=redis.used_cpu_user,/bin/echo "$(/usr/local/bin/redis-cli -a Password123 --no-auth-warning info CPU | grep used_cpu_user: | cut -c15-100)"
UserParameter=redis.total_connections_received,/bin/echo "$(/usr/local/bin/redis-cli -a Password123 --no-auth-warning info Stats | grep total_connections_received: | cut -c28-100)"
UserParameter=redis.total.commands.proc,/bin/echo "$(/usr/local/bin/redis-cli -a Password123 --no-auth-warning info Stats | grep total_commands_processed: | cut -c26-100)"
UserParameter=redis.rejected_connections,/bin/echo "$(/usr/local/bin/redis-cli -a Password123 --no-auth-warning info Stats | grep rejected_connections: | cut -c22-100)"
UserParameter=redis.total_net_input_bytes,/bin/echo "$(/usr/local/bin/redis-cli -a Password123 --no-auth-warning info Stats | grep total_net_input_bytes: | cut -c23-100)"
UserParameter=redis.total_net_output_bytes,/bin/echo "$(/usr/local/bin/redis-cli -a Password123 --no-auth-warning info Stats | grep total_net_output_bytes: | cut -c24-100)"
UserParameter=redis.used_memory_peak,/bin/echo "$(/usr/local/bin/redis-cli -a Password123 --no-auth-warning info Memory | grep used_memory_peak: | cut -c18-100)"
UserParameter=redis.usedmemory.lua,/bin/echo "$(/usr/local/bin/redis-cli -a Password123 --no-auth-warning info Memory | grep used_memory_lua: | cut -c17-100)"
UserParameter=redis.used_memory_rss,/bin/echo "$(/usr/local/bin/redis-cli -a Password123 --no-auth-warning info Memory | grep used_memory_rss: | cut -c17-100)"
